services:
  - type: web
    name: misfits-app
    env: python
    buildCommand: 
      - pip install -r requirements.txt
    startCommand: 
      - python app.py
    preDeployCommand: 
      - python migrate.py
    envVars:
      - key: FLASK_ENV
        value: production
      - key: SECRET_KEY
        value: 'iamdwip'
      - key: UPLOAD_FOLDER
        value: 'uploads'
      - key: DATABASE_URL
        value: 'sqlite:///persistent/misfits.db'
      - key: FLASK_APP
        value: app.py
    staticPublishPath: static
    autoDeploy: true
    disk:
      - mountPath: /persistent
        sizeGB: 1